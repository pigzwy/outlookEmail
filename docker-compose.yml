version: '3.8'

services:
  outlook-mail-reader:
    image: llpig/outlookemail:latest
    container_name: outlook-mail-reader
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - LOGIN_PASSWORD=15695277113
      - SECRET_KEY=change-me-to-a-random-string
      - FLASK_ENV=production
